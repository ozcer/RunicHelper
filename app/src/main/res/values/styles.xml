<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/UiDark</item>
        <item name="colorPrimaryDark">@color/UiDark</item>
        <item name="colorAccent">@color/yellowText</item>
    </style>

    <style name="fontSizeRead">
        <!-- Customize your theme here. -->
        <item name="android:textSize">15dp</item>
        <item name="android:textColor">@color/orangeText</item>
    </style>

    <style name="fontSizeInput">
        <!-- Customize your theme here. -->
        <item name="android:textSize">15dp</item>
    </style>

</resources>
